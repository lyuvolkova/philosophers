#include "philo.h"

int ft_atoi(const char *str)
{
    int i;

    i = 1;
    while(*str == ' ' || *str == '\n' || *str == '\t'\
                    || *str == '\v' || *str == '\f')
        str++;
    if()    
}